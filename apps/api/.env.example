# ===========================================
# AC-Trading API 환경변수 설정
# ===========================================
# 이 파일을 복사하여 .env 파일을 만들고 실제 값을 입력하세요
# cp .env.example .env

# -------------------------------------------
# 서버 설정
# -------------------------------------------
PORT=8080

# -------------------------------------------
# 데이터베이스 (Neon PostgreSQL)
# -------------------------------------------
DATABASE_URL=jdbc:postgresql://your-neon-host.neon.tech/neondb?sslmode=require
DATABASE_USERNAME=neondb_owner
DATABASE_PASSWORD=your-database-password

# -------------------------------------------
# JWT 설정
# -------------------------------------------
# 256비트(32자) 이상의 시크릿 키 (openssl rand -base64 32 로 생성 가능)
JWT_SECRET=your-256-bit-secret-key-here-must-be-at-least-32-characters-long
# Access Token 만료시간 (기본: 1시간 = 3600000ms)
JWT_ACCESS_EXPIRY=3600000
# Refresh Token 만료시간 (기본: 7일 = 604800000ms)
JWT_REFRESH_EXPIRY=604800000

# -------------------------------------------
# AWS Cognito OAuth 설정
# -------------------------------------------
COGNITO_DOMAIN=your-app.auth.ap-northeast-2.amazoncognito.com
COGNITO_CLIENT_ID=your-cognito-client-id
COGNITO_CLIENT_SECRET=your-cognito-client-secret
COGNITO_REDIRECT_URI=http://localhost:8080/api/auth/callback

# -------------------------------------------
# 프론트엔드 URL
# -------------------------------------------
FRONTEND_URL=http://localhost:3000

# -------------------------------------------
# 쿠키 설정
# -------------------------------------------
# 프로덕션에서는 true (HTTPS 필수)
COOKIE_SECURE=false

# -------------------------------------------
# JPA 설정
# -------------------------------------------
# 개발환경: true, 프로덕션: false
JPA_SHOW_SQL=true
